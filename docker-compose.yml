version: "3.9"

services:
  api:
    build: .
    container_name: legal-policy-api
    ports:
      - "8000:8000"

    extra_hosts:
      - "host.docker.internal:host-gateway"

    restart: unless-stopped